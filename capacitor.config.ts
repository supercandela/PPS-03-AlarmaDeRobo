import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ioniccapacitorapp.AprendeConUnToque',
  appName: 'AprendeConUnToque',
  webDir: 'www'
};

export default config;
